# The directory where the counts for each samples are located.

data_dir <- 'salmon'
  
# The sample file must be in CSv format and must have the headers "sample".

desing_file = "design.csv"

# What software created the mappings.

method <- "salmon"

# The output file name.

output_file = "transript_counts.csv"
